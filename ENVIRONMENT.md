# Environment variables

- `DATABASE_URL` — Postgres connection string (Neon).
- `GOOGLE_CLIENT_ID` — OAuth client ID.
- `GOOGLE_CLIENT_SECRET` — OAuth client secret.
- `AUTH_SECRET` — Long random string for NextAuth.

